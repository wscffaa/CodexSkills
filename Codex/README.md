# Codex

这里是 `Codex` 侧的主题导航层。

## 推荐先读

1. `workflow/README.md`
2. `optional/README.md`

## 当前策略

- `workflow/`：默认主线
- `optional/`：专项能力

当前 `Codex` skill 的兼容源树仍保留在：

- `../CodexSkills/`

也就是说：
- 新用户先从这里理解主题
- 现有安装与兼容路径继续从 `CodexSkills/` 提供
